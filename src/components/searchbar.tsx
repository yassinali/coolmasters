import { SearchIcon } from 'lucide-react'
import React from 'react'

const Searchbar = () => {
  return (
    <div>
      <SearchIcon className='size-5 hover:text-shop_light_green hoverEffect cursor-pointer'/>
    </div>
  )
}

export default Searchbar
